on:
  - push
jobs: 
  build:
    runs-on: ubuntu-latest 
    steps:
      - run: echo "Hello"
  setup:
    needs: [ build, docker ]
    runs-on: ubuntu-latest 
    steps:
      - run: echo "Hello"
  checkov:
    needs: setup
    runs-on: ubuntu-latest 
    steps:
      - run: echo "Hello"
  plan:
    needs: checkov
    runs-on: ubuntu-latest 
    steps:
      - run: echo "Hello"
  apply:
    needs: plan
    runs-on: ubuntu-latest 
    steps:
      - run: echo "Hello"
  docker:
    runs-on: ubuntu-latest 
    steps:
      - run: echo "Hello"
  trivy:
    needs: [ docker, setup ]
    runs-on: ubuntu-latest 
    steps:
      - run: echo "Hello"
  dockerise:
    needs: trivy
    runs-on: ubuntu-latest 
    steps:
      - run: echo "Hello"
